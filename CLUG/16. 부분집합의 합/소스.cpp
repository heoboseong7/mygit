#define CRT_SECURE_NO_WARNINGS
#include <stdio.h>

int main()
{
	int n;
	int s;
	scanf("%d, %d", &n, &s);

	int num[25];
	int part[1100000];

	for (int i = 0; i < n; i++)
		scanf("%d", &num);

	for (int i = 0; i < n; i++)
		for (int j = 0; j < 1; j++)
		{

		}
}